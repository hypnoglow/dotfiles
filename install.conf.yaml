- defaults:
    link:
        create: true
        relink: true
        # force: true
    shell:
        stdout: true
        stderr: true

- clean: ['~']

# - shell:
#     - command: echo "Start install"
#       description: pre-hook

- link:
    ~/.dotfiles-mapping: mapping

# - shell:
#     - command: echo "Finish install"
#       description: post-hook
